# data/

- `raw/` — upstream source data. **Not committed** (see ../.gitignore and ../NOTICE).
  Download HYDE via the pipeline; place Cliopatria, Natural Earth, and Maddison
  here. Large and range-fetched.
- `processed/` — small derived outputs (committed): population/area/GDP per
  polity per slice, world totals, transfer matrix.
